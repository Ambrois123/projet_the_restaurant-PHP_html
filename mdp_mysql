<?php 


    $pdo = new PDO("mysql:host=localhost", "project_rest", "Dev_project_restaurant");
    $pdo->setAttribute(PDO::ERRMODE_EXCEPTION, PDO::ATTR_ERRMODE);

    if ($pdo->exec('CREATE DATABASE le_restaurant') !==false) 

        $serveurRest = new PDO("mysql:host=localhost;dbname=le_restaurant", "project_rest", "Dev_project_restaurant");

